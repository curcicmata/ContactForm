Simple web application that save user contact data.

It uses MS SQL server and migrations are run on startup. In appsettings.json database connection string is defined.

For authorization, call Token controller with test/Password123! credentials.
